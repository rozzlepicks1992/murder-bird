{{- if .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-{{ include "murder-bird.name" . }}
  labels:
    {{- include "murder-bird.labels" . | nindent 4 }}
spec:
  rules:
    - host: {{ .Values.ingress.host }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ .Release.Name }}-{{ include "murder-bird.name" . }}
                port:
                  number: {{ .Values.service.port }}
{{- end }}
